# supabase/config.toml

project_id = "your-project-id"

[api]
enabled = true
port = 54321
schemas = ["public", "storage", "graphql_public"]
extra_search_path = ["public", "extensions"]
max_rows = 1000

[auth]
enabled = true
site_url = "http://localhost:5173"
additional_redirect_urls = ["https://your-production-domain.vercel.app"]
jwt_expiry = 3600
enable_refresh_token_rotation = true
refresh_token_reuse_interval = 10
enable_signup = false  # 直接サインアップ禁止（招待のみ）
enable_anonymous_sign_ins = false

[auth.email]
enable_signup = false
enable_confirmations = true
secure_password_change = true

[auth.email.template.invite]
subject = "ORIGIN ADMIN PORTAL - 招待"

[db]
port = 54322
shadow_port = 54320
major_version = 15

[studio]
enabled = true
port = 54323

[functions]
# Edge Functions の環境変数
[functions.env]
SITE_URL = "https://your-production-domain.vercel.app"

[realtime]
enabled = true

[storage]
enabled = false  # ストレージ不使用
